server:
  port: 8083

spring:
  application:
    name: PARENT-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/parent_db
    username: root
    password: Happy@1008

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
